Status: deprioritised

> Ad-hoc Radarr/Sonarr utility scripts - run manually when needed, not actively developed. Do not work on this repo unprompted. Only continue if David explicitly asks.

---

## Ideas (if ever resumed)

- Merge export_custom_cfs.py and export_custom_profiles.py into a single CLI with subcommands
- Add `--dry-run` flag to preview what would be exported before writing files
- Configarr integration: auto-import exported profiles into Configarr config format (see docs/Configarr-research.md)
